<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">E-Commerce</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavDropdown">
      <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
        <li>
          <%= current_user&.user_role&.capitalize %>
        </li>

        <% if current_user&.user_role == 'customer'%>
          <li>
            <%= link_to 'Cart', cart_path, class: "btn btn-outline-warning" %>
          </li>
        <% end %>

        <li class="nav-item">
          <% if user_signed_in? %>
            <%= button_to 'Log out', destroy_user_session_path, method: :delete, data: {turbo: false}, class: "btn btn-outline-warning" %>
          <% else %>
            <%= link_to 'Sign up', new_user_registration_path, class: "btn btn-outline-warning" %>
          <% end %>
        </li>
        </ul>
    </div>
  </div>
</nav>